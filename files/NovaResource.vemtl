<?php
<# TEMPLATE VARIABLES #>
<% let crudPluginData = this.pluginData.cruds[this.crud.id] %>
<% let crudModelName = this.crud.model.name %>
<% let modelsNamespace = this.projectHelper.getModelsNamespace() %>
<####>

namespace App\Nova;

use Laravel\Nova\Fields\ID;
use Illuminate\Http\Request;
use Laravel\Nova\Http\Requests\NovaRequest;
<% if(this.crudHasTextInputs) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Text') $>;
<% } %>
<% if(this.crud.hasSelectInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Select') $>;
<% } %>
<% if(this.crud.hasImageInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Image') $>;
<% } %>
<% if(this.crud.hasFileInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('File') $>;
<% } %>
<% if(this.crud.hasNumericInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Number') $>;
<% } %>
<% if(this.crud.hasTextareaInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Textarea') $>;
<% } %>
<% if(this.crud.hasDateInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Date') $>;
<% } %>
<% if(this.crud.hasDatetimeInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Datetime') $>;
<% } %>
<% if(this.crud.hasPasswordInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Password') $>;
<% } %>
<% if(this.crud.hasHiddenInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Hidden') $>;
<% } %>
<% if(this.crud.hasCheckboxInputs()) { %>
use Laravel\Nova\Fields\<$ this.fieldName('Boolean') $>;
<% } %>
<###>
<% for(let relationship of this.crudModelRelationships) { %>
    use Laravel\Nova\Fields\<$ relationship.type.case('pascalCase') $>;
<% } %>

class <$ crudModelName $> extends Resource
{
    /**
     * The model the resource corresponds to.
     *
     * @var string
     */
    public static $model = \<$ modelsNamespace $>\<$ crudModelName $>::class;

    /**
     * The single value that should be used to represent the resource when being displayed.
     *
     * @var string
     */
    public static $title = '<$ this.crud.model.getLabelFieldName() $>';

    /**
     * The columns that should be searched.
     *
     * @var array
     */
    public static $search = [
        '<$ this.crud.model.getLabelFieldName() $>',
    ];

    /**
     * Get the fields displayed by the resource.
     *
     * @param  \Illuminate\Http\Request  $request
     * @return array
     */
    public function fields(Request $request)
    {
        return [
            ID::make('id')->sortable(),

            <% if(this.crud.inputs.length && crudPluginData.inputs) { %>
                <# Crud inputs #>
                <% for(let input of this.novaInputs) { %>
                    <###>
                    <$ this.getTypeForNova(input) $>::make('<$ input.name.case('pascalCase') $>')
                        <% if(input.hasValidation() || input.hasUpdateValidation()) { %>
                            <###>
                            <% if(!input.hasDifferentUpdateValidation()) { %>
                                ->rules(<$ this.getValidationForNova(input) $>)
                            <% } else { %>
                                ->creationRules(<$ this.getValidationForNova(input) $>)
                                ->updateRules(<$ this.getUpdateValidationForNova(input) $>)
                            <% } %>
                        <% } %>
                        <###>
                        <% if(input.isSelect()) { %>
                        ->searchable()
                        ->options([
                            <% for(let item of input.items) { %>
                                '<$ item.value $>' => '<$ item.label $>',
                            <% } %>
                        ])
                        ->displayUsingLabels()
                        <% } %>
                        <###>
                        <% if(input.readOnly) { %>
                        ->readonly(optional($this->resource)->trashed())
                        <% } %>
                        <###>
                        <% if(input.placeholder) { %>
                        ->placeholder('<$ input.placeholder $>')
                        <% } %>
                        <###>
                        <% if(input.defaultValue) { %>
                        ->default('<$ input.defaultValue $>')
                        <% } %>
                        <###>
                        <% if(!input.onIndex) { %>
                        ->hideFromIndex()
                        <% } %>
                        <###>
                        <% if(!input.onShow) { %>
                        ->hideFromDetail()
                        <% } %>
                        <###>
                        <% if(!input.onCreate) { %>
                        ->hideWhenCreating()
                        <% } %>
                        <###>
                        <% if(!input.onUpdate) { %>
                        ->hideWhenUpdating()
                        <% } %>,

                <% } %>
            <% } %>

            <% for(let relationship of this.crudModelRelationships) { %>
                <###>
                <% if(crudPluginData.relationships[relationship.id]) { %>
                    <% let relInput = relationship.inputs ? relationship.inputs[0] : null %>
                    <###>
                    <$ relationship.type.case('pascalCase') $>::make('<$ this.getRelationshipModelName(relationship) $>')
                    <###>
                    <% if(relInput && relInput.field.nullable) { %>
                        ->nullable()
                    <% } %>,

                <% } %>
            <% } %>
        ];
    }

    /**
     * Get the cards available for the request.
     *
     * @param  \Illuminate\Http\Request  $request
     * @return array
     */
    public function cards(Request $request)
    {
        return [];
    }

    /**
     * Get the filters available for the resource.
     *
     * @param  \Illuminate\Http\Request  $request
     * @return array
     */
    public function filters(Request $request)
    {
        return [];
    }

    /**
     * Get the lenses available for the resource.
     *
     * @param  \Illuminate\Http\Request  $request
     * @return array
     */
    public function lenses(Request $request)
    {
        return [];
    }

    /**
     * Get the actions available for the resource.
     *
     * @param  \Illuminate\Http\Request  $request
     * @return array
     */
    public function actions(Request $request)
    {
        return [];
    }
}
